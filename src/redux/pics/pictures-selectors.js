// import { createSelector } from 'react-redux';

export const getItems = state => state.items;
export const getLargeImageURL = state => state.largeImageURL;
export const getIsModalOpen = state => state.isModalOpen;
